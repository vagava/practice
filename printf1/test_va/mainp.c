#include <stdio.h>
#include <stdlib.h>
#include "../holberton.h"
/**
 * main - Entry point
 *
 * Return: 0 on success, error code otherwise
 */
int main(void)
{
	_printf("%d", 1024);
	printf("%d", 1024);
	fflush(stdout);
	return (0);
}